<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:TimeManager.ViewModel;assembly=TimeManager"
             x:Class="TimeManager.AllHistory" Title="Complete History" BackgroundColor="#FFFFFF">
  <ContentPage.BindingContext>
    <viewModel:AllHistoryViewModel />
  </ContentPage.BindingContext>
  <ContentPage.Resources>
    <ResourceDictionary>
      <Style x:Key="labelStyle" TargetType="Label">
        <Setter Property="TextColor" Value="Black" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="VerticalOptions" Value="Center" />
      </Style>
      <DataTemplate x:Key="TimeLineTemplate">
        <ViewCell>
          <Label Text="{Binding TimeDiffText}" Style="{StaticResource labelStyle}" />
        </ViewCell>
      </DataTemplate>
    </ResourceDictionary>
  </ContentPage.Resources>
  <Grid>
    <ListView ItemsSource="{Binding AllSessions}" ItemTemplate="{StaticResource TimeLineTemplate}"/>
  </Grid>

</ContentPage>